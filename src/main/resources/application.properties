# =======================================
# COMMON APPLICATION SETTINGS
# =======================================
# By default, the 'local' profile is active.
spring.profiles.active=local

# --- Custom Security Properties ---
# A comma-separated list of email addresses that are allowed to access the API.
app.security.allowed-emails=daniel4bar@gmail.com,ewelina4bar@gmail.com

app.security.firebase_project_id=user1@example.com,admin@example.com

# --- Hibernate Configuration (common for all profiles) ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
